export default class User {

    constructor(first, last, birth, fac, grade) {
        this.first = first;
        this.last = last;
        this.birth = birth;
        this.fac = fac;
        this.grade = grade;
    }
}